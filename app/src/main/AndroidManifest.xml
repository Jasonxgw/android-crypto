<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tsou.com.encryption">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AES.AESECBActivity" />
        <activity android:name=".activity.AES.AES128Activity" />
        <activity android:name=".activity.AES.AESCBCActivity" />
        <activity android:name=".activity.RSA.RSAAsymmetricActivity" />
        <activity android:name=".activity.DES.DESActivity" />
        <activity android:name=".activity.MD5.MD5Activity" />
        <activity android:name=".activity.Base64.Base64Activity" />
        <activity android:name=".activity.OR.ORActivity" />
        <activity android:name=".activity.SP.SPActivity"></activity>
        <activity android:name=".activity.AndroidKeyStore.AndroidKeyStoreActivity" />
        <activity android:name=".activity.AndroidKeyStoreRSA.AndroidKeyStoreRSAActivity" />
        <activity android:name=".tink.TinkActivity"/>
    </application>

</manifest>